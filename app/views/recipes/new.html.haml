.contents
  .contents__new
    = form_with(model: @recipe, local: true) do |form|
      %h3.contents__new__title 投稿する
      = form.text_field :name, placeholder: "Nickname", class: "contents__new__text"
      = form.text_field :image, placeholder: "Image Url", class: "contents__new__text"
      = form.text_area :text, placeholder: "text", rows: "10"
      = form.submit "SEND", class: "contents__new__send"